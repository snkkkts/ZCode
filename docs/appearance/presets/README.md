# 配色示例

这里保存 7 套无图片的 JSON 配色示例，原文件仍保留在本机 `F:\Zcode-dev\theme-presets`。

复制某份 JSON 到“外观配置代码”即可应用。当前是合并语义：示例未写出的字段继续保留；若示例显式写了 `backgroundImage: ""`，会清除背景。字体名称需在本机安装，否则使用回退字体。

这 7 套配色也已内置在设置页“主题方案 → 内置配色”中，可一键套用（只替换颜色）；这里的 JSON 另含作者本机字体设置，粘贴时会一并写入。均不改变代码语法高亮主题。完整规则见 [配置说明](../CONFIGURATION.md)。
